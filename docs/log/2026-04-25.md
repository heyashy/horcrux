# Change Log — 2026-04-25

## Summary
Reframed the project from "weekend learning" to "weekend lab" for portfolio
positioning. Strengthened repo hygiene for public release (corpus and derived
data excluded). Wrote the toolchain tour as a 10-phase lab journal. Drafted
two new ADRs covering decisions made in the previous session that hadn't yet
been formally captured.

## Changes
- [CHORE] `.gitignore` — strengthened to exclude PDFs, derived data, model
  caches, secrets, and editor noise. Verified `git check-ignore` catches
  `*.pdf`, pasted images, and `.env`.
- [CHORE] `docs/horcrux_system_design.md` — tone pass from "learning project"
  to "weekend lab"; added CLI surface section describing the Rich-based
  console layer (logging, progress bars, ResearchReport rendering,
  clarification prompts).
- [CHORE] `docs/horcrux_system_design.md` — added `litellm`, `rich`,
  `httpx`, and `langgraph-checkpoint-sqlite` to dependencies.
- [CHORE] `docs/adr/pending/ADR-0001-...` — tone pass; reframed unpinned
  pydantic-ai trade-off as "acceptable for time-boxed lab" rather than
  "for a learning project".
- [ADR-0002] LiteLLM proxy for model routing — pending. Captures the
  decision to run LiteLLM in proxy mode (vs library mode), with rollback
  steps if the proxy proves to be friction.
- [ADR-0003] Interactive clarification via LangGraph interrupts — pending.
  Captures the decision to use `interrupt()` + SQLite checkpointer instead
  of a simple CLI re-invocation loop, with rollback path.
- [CHORE] `docs/adr/MANIFEST.md` — appended ADR-0002 and ADR-0003.
- [CHORE] `docs/lab/toolchain-path.md` — new file. 10-phase lab walkthrough
  (Phase 0 repo skeleton through Phase 9 stretch experiments). Each phase
  includes claim, setup, walkthrough, exercise, gotchas, and "got it"
  signal. Data-first ordering (cleansing → modelling → ingestion →
  durability → agents → graph → routing/observability → human-in-the-loop).
  Lives in `docs/lab/` to namespace lab-specific artefacts away from
  architectural docs (system design, ADRs).
- [CHORE] `README.md` — populated as the recruiter-facing landing page.
  Architecture mermaid diagrams inline; tool-by-tool earned-value table;
  three-terminal run instructions; honest scoping section.

- [CHORE] `docs/lab/toolchain-path.md` — renamed from `docs/toolchain-tour.md`
  and moved into `docs/lab/` to namespace lab artefacts away from
  architectural docs. Title inside file changed "Toolchain Tour" → "Toolchain Path".
- [CHORE] `data_lake/` — created. Holds raw, unstructured source data
  (the corpus PDF). Tracks only `data_lake/README.md`; everything else
  excluded by `.gitignore`.
- [CHORE] Renamed `harry_potter_complete.pdf` → `data_lake/corpus.pdf`.
  More portfolio-friendly filename in a public repo, and out of the repo
  root. Updated three exploratory scripts in `scripts/` to point at the
  new path.
- [CHORE] Updated PDF path references in `README.md`,
  `docs/horcrux_system_design.md`, and `docs/lab/toolchain-path.md`.
- [CHORE] Verified via `git check-ignore` and `git add --dry-run .` that
  the corpus PDF cannot be committed. Two layers of protection:
  `data_lake/*` excludes everything in the lake by default; `*.pdf`
  globally excludes any PDF anywhere.

- [ADR-0004] Centralised config via pydantic-settings — pending. Single
  source of truth for env vars and tunables; fail-fast validation at import;
  `SecretStr` for credential leak protection. Captures the rationale,
  rejected alternatives (hand-rolled singleton, `os.getenv` scatter,
  python-dotenv, omegaconf, function-arg DI), and rollback path.
- [CHORE] `docs/horcrux_system_design.md` — added Config singleton section
  with the full `Settings` shape, usage examples, parallel-readers note
  (Python `settings` vs LiteLLM proxy YAML), and test override pattern.
- [CHORE] `docs/horcrux_system_design.md` — Project structure tree updated
  to include `config.py` and split out `chapters.py`, `characters.py`,
  `workflows.py`, `activities.py`, `worker.py` from the original sketch.
- [CHORE] `docs/horcrux_system_design.md` — PydanticAI agents section
  updated. Model strings now `settings.litellm.haiku_alias` /
  `settings.litellm.sonnet_alias`, resolved by the LiteLLM proxy from
  `litellm_config.yaml`.
- [CHORE] `docs/horcrux_system_design.md` — added `pydantic-settings` to
  dependencies; removed `python-dotenv` (pydantic-settings handles `.env`
  natively, no need for both).
- [CHORE] `horcrux/__init__.py` — empty package marker.
- [CHORE] `horcrux/config.py` — actual implementation of the singleton.
  Five nested config blocks (Qdrant / Temporal / LiteLLM / LangSmith /
  Embedding) plus top-level required `anthropic_api_key`. Imports will
  fail fast if `.env` lacks the API key.
- [CHORE] `.env.example` — populated with required + optional fields.
  Comments explain the `__` nested delimiter syntax and link to ADR-0004.
- [CHORE] `docs/adr/MANIFEST.md` — appended ADR-0004.
- [CHORE] `CLAUDE.md` — full rewrite. Replaced the generic AGENTS.md
  template with a project-specific engineering charter. Removed irrelevant
  sections (Frontend / Svelte, AWS / Terraform / Bitbucket CI/CD, AWS
  observability, RUNBOOK requirements, multi-tenant security). Added:
  stack-with-roles table; architectural non-negotiables (strict RAG,
  config singleton, two-collection chunking, LiteLLM proxy, interrupt-based
  clarification, conviction calibration) each citing the relevant ADR;
  tool-specific rules sections for Temporal, LangGraph, PydanticAI,
  LiteLLM, Qdrant, and Rich; explicit "libraries NOT used" list (SQLModel,
  Tenacity, Sentry, python-dotenv, boto3); revised project structure tree
  matching the actual `horcrux/` package; revised Makefile targets (no
  Terraform); revised Definition of Done with project-specific invariants;
  out-of-scope section to make the lab boundaries explicit. ADR + Change
  Log processes preserved verbatim.

## ADRs Progressed
- ADR-0002 created in `pending`.
- ADR-0003 created in `pending`.
- ADR-0004 created in `pending`.
